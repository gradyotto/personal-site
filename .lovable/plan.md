

## Changes

Update the "Supply Chain Graph" project card in `src/components/WorkbenchSection.tsx`:
- Rename title from "Supply Chain Graph" to **"Commodity Pulse Tool"**
- Add `url: "https://pulse.tibermfg.com"` so the card links out (same pattern as Tiber)

Single file modified: `src/components/WorkbenchSection.tsx`

